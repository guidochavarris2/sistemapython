# .nixpacks
setup:
  - python3
  - gcc

install:
  - python -m venv --copies /opt/venv
  - . /opt/venv/Scripts/activate
  - pip install -r requirements.txt

start:
  - gunicorn app:app --bind 0.0.0.0:$PORT
